{% extends 'base.html.twig' %}

{% block title %}Tipo de Actividad{% endblock %}

{% block body %}


 <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
    <h1>Tipo de Actividad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Denominación</th>
                <td>{{ n_tum.denomination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_n_t_a_index') }}"><i class="fa fa-tasks"></i></a>
    {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_n_t_a_edit', {'id': n_tum.id}) }}"><i class="fa fa-edit"></i></a>

    {{ include('nta/_delete_form.html.twig') }}

    {% endif %}


        </div>
</section>
    </div>
{% endblock %}
