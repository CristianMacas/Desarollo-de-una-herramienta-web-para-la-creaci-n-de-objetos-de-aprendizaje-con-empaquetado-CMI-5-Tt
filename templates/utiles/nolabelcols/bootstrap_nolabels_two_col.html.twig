{% block form_row -%}
    <div class="col-lg-12 col-sm-6 {% if (not compound or force_error|default(false)) and not valid %} has-error{% endif %}">
        {{- form_widget(form) -}}
    </div>
{%- endblock form_row %}